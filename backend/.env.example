# Backend - Patrimônio
# Copie para .env e preencha

PORT=3001
# Host para escutar conexões (0.0.0.0 = todas as interfaces de rede)
HOST=0.0.0.0

# CORS: URLs permitidas separadas por vírgula
# O sistema automaticamente permite IPs locais (192.168.x.x, 10.x.x.x, 172.16-31.x.x)
# Para acesso pela rede, adicione o IP da máquina aqui ou deixe vazio para permitir todos os IPs locais
CORS_ORIGIN=http://localhost:5173,http://localhost:8080

# Banco local para testes
# Aqui usaremos SQLite em arquivo local, isolado do ambiente hospitalar.
# Em produção/homologação, configure PostgreSQL em outro .env apropriado.
DATABASE_URL="file:./dev.db"

# JWT (Fase 1)
JWT_SECRET=seu-secret-forte-aqui
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
